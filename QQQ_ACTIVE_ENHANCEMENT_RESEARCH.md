# 受约束 QQQ 主动增强研究

`qqq_active_enhancement_research.py` 是隔离、只读的 point-in-time 研究。它用
Yahoo 美股日线和 SEC Company Facts 验证慢频横截面选股 alpha，并把 OKX
TRADFI 公共行情单独记录为锁模后的前瞻执行样本。脚本不加载 `.env`，不访问
账户或私有端点，不下单，也不修改服务和实盘配置。

## 固定设计

- 组合以 100% QQQ 为 beta 核心，单股只做美元中性的主动多空偏离。
- 月末调仓为主版本；双周频只作敏感性诊断。持仓跨日，不在每日收盘清仓。
- 12-1 动量、质量、价值和低残差波动率先分别横截面标准化，再等权合成；
  不训练模型，不根据测试期删因子或挑股票。
- SEC 年报事实只有在实际 `filed` 日期严格早于调仓日时才进入信号。后续修订
  不会回填到首次披露以前。
- 主动权重同时对行业、beta 和市值中性；单股绝对权重上限 1.5%，主动 gross
  上限 20%，事前跟踪误差上限 3%。
- 基础成本为主动换手每次单边 5 bps、空头年化 1%；压力成本为 10 bps 和 3%。

## 最终固定样本

最终报告位于 `reports/qqq_active_enhancement/qqq-pit-20260808-v5/report.md`。
Nasdaq 2026-08-06 官方成分与 OKX TRADFI 合约的交集为 40 只；排除外国注册、
映射不清、历史不足或缺少 SEC 数据后，32 只进入现货代理回测。月频主版本覆盖
2017-09-01 至 2026-08-07，按 60%/20%/20% 顺序切分，不做测试后调参。

样本外 2024-10-22 至 2026-08-07：

- 完整研究宇宙基础成本主动年化 2.97%，实现跟踪误差 1.89%，IR 1.57；压力
  成本主动年化 2.81%。
- 中性化综合 Rank IC 为 0.0599，22 期中 63.6% 为正。三个月块 bootstrap 的
  主动年化 95% 区间为 1.00% 至 6.58%。
- 双周诊断的基础/压力主动年化为 2.53%/2.36%，没有出现频率翻转。

## 当前可执行子集与锁模

报告生成时，QQQ 和 12 只具备足够现货历史的股票合约通过 5 bps 价差与 500 万
USDT 估算 24h 换手门槛。冻结子集为 `AMD`、`GOOGL`、`INTC`、`LITE`、
`MRVL`、`MSTR`、`MU`、`NVDA`、`PLTR`、`RKLB`、`TSLA`、`WDC`。

该子集的月频样本外基础/压力主动年化为 2.55%/2.46%，跟踪误差 1.45%，基础
IR 1.76，中性化综合 IC 0.1039。研究因此允许锁定模型，开始全新的公共合约
前瞻观察：

- `locked_model.json` 冻结模型 `qqq-pit-1fda3d7e14f137bf`，最近一个已确认完成的
  月末为 2026-07-31；文件明确 `paperOrLiveAuthorized: false`。
- `forward_snapshot_0001.json` 是第一份公共合约价差、换手、上市时间和资金费
  快照；没有账户或订单访问。

这不是仿真或实盘放行。当前子集平均主动 gross 只有约 7.6%、测试期跟踪误差
1.45%，低于最初 2%--4% 目标，反映单股上限和合约覆盖仍在约束组合。前瞻期
必须检验真实合约价差、深度、资金费、基差和标的映射稳定性；不得用前瞻结果
继续改模型后仍称其为同一份样本。

## 运行

```bash
PYTHONPATH=. .venv/bin/python qqq_active_enhancement_research.py \
  --output-dir reports/qqq_active_enhancement/qqq-pit-20260808-v5
```

默认复用 `data/qqq_active_enhancement/` 的公开缓存。只有明确需要更新相应数据时
才使用 `--refresh-universe`、`--refresh-prices` 或 `--refresh-sec`。刷新当前
合约快照可能改变执行子集，因此新的锁模必须使用新报告目录，不能覆盖既有前瞻
样本。

## 数据边界

- 历史宇宙来自今天的 Nasdaq-100 与今天存在的合约交集，存在当前成分和当前
  合约选择偏差；尚未重建完整历史指数成员和退市证券主文件。
- SEC `filed` 通常只有日期而非统一的可交易时间，本研究用严格前一日规则保守
  滞后；行业使用当前 SEC SIC，历史行业迁移没有完整重建。
- Yahoo 复权日线适合初步慢因子验证，但不能替代机构级点时价格/公司行动数据。
- 历史合约 K 线不能恢复 bid/ask、深度、排队、资金费路径或基差成交。上述内容
  只能从锁模后的新前瞻快照判断。
