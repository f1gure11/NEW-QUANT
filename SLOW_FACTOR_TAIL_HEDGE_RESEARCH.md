# 慢因子与尾部保险研究

`slow_factor_tail_hedge_research.py` 是隔离的只读研究，不读取 `.env`、账户或私有接口，也不发送订单或改变实盘配置。

固定比较三组小时级多周期动量：

- 原周期：6/12/24/48 小时；
- 适度放宽：12/24/48/96 小时；
- 慢周期对照：24/48/96/168 小时。

尾部保险只用于适度放宽方案：在每个季度到期前 30 天，同时买入约 15% OTM Put 与 Call，覆盖 2 倍名义暴露的 25%、50% 或 100%。这样既覆盖多头暴跌，也覆盖趋势模型翻空后的逼空风险。基础期权滑点为每端 5%，压力为每端 10%。

运行：

```bash
PYTHONPATH=. .venv/bin/python slow_factor_tail_hedge_research.py \
  --output-dir reports/slow_factor_tail_hedge/btc-eth-20260807
```

报告重点看样本外中位收益、中位最大回撤、最差最大回撤、信号切换次数以及权利金占权益。公共历史没有 bid/ask 或连续可靠的深度虚值期权成交，因此逐小时估值固定使用入场成交反推 IV；结果只能用于结构筛选，不能授权自动买入或实盘。
