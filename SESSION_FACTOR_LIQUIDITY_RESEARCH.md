# 分时段短因子与慢因子流动性研究

`session_factor_liquidity_research.py` 是隔离的只读研究，不读取账户或私有接口，不发送订单，也不修改任何实盘配置。

- 美洲盘：纽约工作日 09:30–16:00，比较 15/30/60 根短窗和 240 根原周期对照；训练标签与因子回看必须完整位于同一个美洲盘。比较全部 Ridge、稳定 IC、mRMR、分块 ElasticNet，以及 raw、因果 EWMA、因果 Kalman。短窗同时绑定更短的预测与最长持有，以检验能否在有限选择期内增加交易样本。
- 非美洲盘：只测试 12/24/48 小时慢 VWAP 的被动流动性提供。先在模型训练段从 96 个参数中预筛 12 个，再在选择段固定一个；进入美洲盘即清空库存。
- 两个时段独立选择并在边界归零，不允许通过验证或测试结果事后换候选。

Maker 回测使用完成的 5 分钟 K 线，报价从下一根生效，价格必须穿过报价才计成交，同根双边触发只保留盯市结果更差的一边。K 线无法识别真实排队位置，因此结果无论好坏都不能授权仿真或实盘。

固定数据截止运行：

```bash
PYTHONPATH=. .venv/bin/python session_factor_liquidity_research.py \
  --end-time 2026-08-07T16:32:13.913Z \
  --output-dir reports/session_factor_liquidity/btc-eth-20260807
```
